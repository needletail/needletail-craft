{% import '_includes/forms' as forms %}

{% set assets = element.groups %}

{{ forms.selectField({
    label: "Asset Source" | t('needletail'),
    instructions: 'Choose the asset source you want to index.' | t('needletail'),
    id: 'elementData-' ~ elementType,
    name: 'elementData[' ~ elementType ~ ']',
    options: craft.needletail.getSelectOptions(assets),
    value: bucket.elementData[elementType] ?? '',
    required: true,
}) }}
